# 共通ルール：状態・公開・権限

[READMEへ](../../README.md) · [操作とページ](product.md) · 次は[内部処理](architecture.md)

この文書が共通仕様の説明先です。ページ別の文書はここを参照し、状態表や公開条件を複製しません。根拠は[変更対応表](change-map.md#cross-cutting)から追えます。以下の実装説明を、未承認の仕様変更を正当化する根拠にはしません。

<a id="allergens"></a>
## 品目、保存状態、表示上の分類は別物

「品目」は卵・乳などの対象、「状態」はそのメニューに対する登録内容です。現行の `ALLERGEN_MASTER` は29品目。`lib/allergens.ts` は品目を9品目の「特定原材料」と20品目の「特定原材料に準ずるもの」に分けています。これは**現在のアプリ内分類**の説明であり、最新の法令への適合を確認した記述ではありません。件数を増減する変更では、定義だけでなくDBマスタ、固定件数の検証、マイグレーション、テストも調べます。

DBの `AllergenStatus` は4状態です。

| 内部状態 `status` | 現在の基本表示 | 意味・注意 |
| --- | --- | --- |
| `CONTAINS` | 含む | そのメニューに含む登録 |
| `FREE` | 原材料に含まない登録 | 厨房の交差接触まで確認済みという意味ではない |
| `MAY_CONTAIN` | 含む可能性あり・要確認 | 不確実性を残した登録。公開可能だが注意表示する |
| `UNKNOWN` | 未設定 | 信頼できる判断が入力されていない。表示箇所によって「未確認」と表現する |

レコード欠損は表示・公開判定で `UNKNOWN` に補完します。DBの初期値も `UNKNOWN` です。`null`、空欄、不正値を `FREE` にしてはいけません。API入力は状態の値を検証し、知らない品目を拒否します。

画面上の `effectiveRisk` は、保存状態に同じ店舗の別の公開登録を加味した**導出値**です。`FREE` の品目が別の公開可能メニューで `CONTAINS` なら `STORE_HANDLED` となり、別登録に含む情報がある旨を補足します。`CONTAINS`、`MAY_CONTAIN`、`UNKNOWN` は元の意味を保持します。`STORE_HANDLED` をDBの状態として保存しません。厨房を調査した結果でもありません。

選択品目の結果では含む・可能性あり・店舗内の補足・未設定を隠しません。全選択が確認条件を満たさない限り、問題がないと受け取れる要約にしません。内部の見た目用分類に `safe` という名前が残っていても、食品安全の保証という意味ではありません。選択なしも「含まない」ではありません。

<a id="publication"></a>
## メニューの公開と公開側の取得

1. 名前が空でない。
2. DBのアレルゲンマスタがコードの品目集合と一致し、欠損・余分・重複がない。現在の検証には29件という固定判定もある。
3. 現行マスタ全品目の状態が入力され、`UNKNOWN` やリンク欠損がない。`MAY_CONTAIN` は許可する。

上記を `getMenuPublishValidationErrors` / `isMenuPublishable` で確認します。公開フラグ `isPublished=true` だけで公開側に渡しません。公開一覧・詳細・メニューAPIは稼働店舗 `isActive=true` と公開フラグを確認し、取得後にも公開可能性を検証します。店舗一覧には公開可能なメニューが1件以上ある店舗だけを渡します。稼働店舗の詳細ページはメニュー0件の表示もあります。

新規・編集画面は公開できない理由を示し、APIへ明示的に不完全な公開要求が来た場合も拒否します。既存の公開メニューに、公開フラグを指定しない部分更新で `UNKNOWN` を追加した場合は、更新結果を非公開にします。部分更新で省略した既存状態を一律に消す処理ではありません。

さらにDBトリガー（DB更新時に自動で走る処理）が、不完全な公開データを非公開にします。既存の `20260622000000_auto_unpublish_incomplete_menus` マイグレーションが根拠です。アプリ側チェックの代わりではありません。現行DBへの適用状況をコードだけで断定できません。

原材料変更時の確認ダイアログは入力者の見直しを促すUIです。原材料から自動判定したり、確認履歴・食品検査の証跡を作ったりする機能ではありません。公開条件や注意文の緩和には製品方針の明示的な判断が必要です。

<a id="ownership"></a>
## 認証・所有権・運営権限

**認証**は誰がログインしているかの確認、**認可**はその人が対象を操作できるかの確認です。

- Clerkのサーバー側セッション → `User.clerkUserId` → `Shop.ownerClerkUserId` と `isActive` の順に店舗を解決します。通常のユーザー取得は読み取り専用です。ログイン確認だけで旧メールアドレスのユーザーを自動連携しません。
- 管理メニューの取得・更新・削除はメニューIDと解決済み店舗IDで絞ります。リクエスト本文の `shopId` や所有者フラグを信用しません。管理ページは未認証でログインへ、店舗未割当なら登録入口へ移動します。APIでは未認証401、店舗なし403、他店舗の対象404を使います。
- 運営管理者はClerkの `publicMetadata.role === "admin"` で確認します。招待管理権限と、個別店舗の編集権限は同一ではありません。管理者ロールだけで任意店舗のメニュー更新APIが使えるわけではありません。
- `PORTFOLIO_MODE=true` では更新に追加制限があります。運営管理者、許可したアプリユーザーIDなどは更新可。許可判定には設定値だけでなくコード内の既定IDも含まれます。これは所有権確認を省略する許可ではありません。
- `proxy.ts` は管理・認証ルートでClerk状態を扱う入口です。これだけで対象データの所有権を保証しません。ページ・APIのサーバー側チェックも必要です。
- 管理更新では同一オリジン（同じサイトからの要求）の確認を行います。ログイン補助などの回数制限はプロセス内メモリの方式で、複数サーバー共通の制限ではありません。

自己登録は `getAdminRegistrationMode()` が常に `disabled` を返します。旧環境変数を `open` に変えても再開しません。店舗作成・割当は運営の招待と受諾を通します。招待受諾だけは、Clerkから取得したメールと有効招待を照合して、未連携の既存ユーザーを連携する処理があります。通常ログインとの違いに注意してください。

現行DBは `Shop.userId` と `ownerClerkUserId` がそれぞれ一意で、1店舗の複数担当者を表すメンバー表はありません。「招待」は複数管理者の追加機能を保証しません。

<a id="images"></a>
## 画像・入力・エラーの共通境界

画像APIは認証、店舗所有権、更新許可を確認し、JPEG・PNG・WebP・GIF・AVIF、5MiB以下を受け付けます。MIMEタイプ（ファイル形式を示す値）とサイズの検証であり、画像内容の安全性を全面検査する仕組みではありません。Vercel Blob（画像ファイルの保存サービス）へアップロードし、DBにはURLと表示設定を保存します。

URLはHTTPS、許可したBlobストア、自店舗の保存パスを検証します。枠は正方形／横長、表示は `cover`（枠を埋める）／`contain`（全体を収める）、拡大率50〜250%、位置0〜100%の範囲です。既存URLも表示前に検証し、不許可なら画像として使いません。アップロード成功とDB保存成功は別段階で、古い画像の自動回収は未整備です。

外部入力はAPIでZod（入力の型と条件を検査するライブラリ）や既存の検証関数へ通します。画面の必須表示だけでは保証しません。APIのエラー形式は統一されておらず、主に `error` / `message` を画面の共通処理で取り出します。すべての障害が503になるわけではありません。契約を変更する場合は呼び出し元も調べます。

`AuditLog` は認証・招待・店舗・メニュー等の管理操作を記録します。食品の確認履歴を利用者へ提供する機能とは別です。秘密情報・個人情報・実データを文書や検証ログに写さず、監査ログの内容も無条件に貼り付けないでください。
