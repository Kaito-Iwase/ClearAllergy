# ClearAllergy 開発エージェント向け指示

## 文書の入口と変更の完了条件

- 初めて読む場合は [READMEの読む順番](README.md#reading-order) から進む。コード変更前には [変更対応表](docs/guide/change-map.md) と関連仕様を読み、呼び出し元も検索して影響範囲を確認する。
- 「影響を受ける範囲」「実際に変更する範囲」「確認のみの範囲」を分け、依頼と無関係な変更を広げない。
- ページ・表示・入力・状態・権限・API・データ・起動・テスト・運用が変わったら、関連文書を同じ作業で更新する。追加・移動・改名・削除時は、参照・目次・対応表も更新する。
- 古い説明への追記だけで済ませず、本文全体を整合させる。現行仕様と過去の記録を区別し、実装上の動作だけを理由に不具合を承認済み仕様にしない。
- 説明・参照も変わらない文書は無理に変更せず、更新不要の理由を報告する。AGENTS.mdは作業ルール・コマンド・参照先が変わる場合に更新し、変更履歴を蓄積しない。
- 完了前にコード・関連テスト・文書、リンク・コード参照を照合する。必要な文書更新が残る場合は完了扱いにしない。文書変更と動作検証は別に報告する。

詳しい執筆・保守手順とチェックリストは [文書の保守](docs/guide/maintenance.md)、共通仕様の説明先は [状態・公開・権限](docs/guide/rules.md)、起動・検証は [開発手順](docs/guide/development.md) を参照する。

## 作業環境と開始時の確認

開発環境はWSLからWindowsへ移行済みです。今後の作業先は必ず次のフォルダを使用してください。

`C:\Users\kaito\Documents\Github\ClearAllergy`

- PowerShellを前提とし、コマンドは上記フォルダで実行する。
- WSL側の元フォルダや、次のバックアップは作業先にしない。明示的な依頼なしに変更・削除しない。
  - `C:\Users\kaito\Documents\Github\ClearAllergy-before-wsl-return-20260920`
  - `C:\Users\kaito\Documents\Github\ClearAllergy-wsl-import-20260920`
- Windows用の依存パッケージとPrisma Clientは移行時に準備済み。問題が起きたら現在の状態を調べ、再インストールや再生成を最初の対応にしない。
- このリポジトリには `core.longpaths=true` が設定済み。Gitのグローバル設定を勝手に変更しない。
- ファイルの読み書きはUTF-8を明示し、既存の改行・コメント・設定を不要に変更しない。

編集前に、現在の状態を確認する。

```powershell
Get-Location
git branch --show-current
git status --short
git log -1 --oneline
```

移行時のブランチは `develop` だが、毎回実際のブランチを確認する。想定と異なる場合も勝手に切り替えない。過去のコミット番号やテスト結果を現在の状態とみなさない。

## 製品の目的

ClearAllergyは、飲食店によるアレルゲン情報の公開と、利用者による情報の確認を支援します。不確かな情報を安全の保証と誤認させないことを優先します。

- 誤った安心や対象の取り違えを防ぐ。
- 確認済み・不確実・未入力を明確に区別する。
- 公開情報を読みやすくする。
- 店舗側が実務で入力・更新しやすくする。
- 不確実な点がある場合は店舗への確認を促す。

実装の簡略化によって、これらの意味を変えないでください。

## 判断の根拠と調査

コードを変更する前に現在のリポジトリを確認し、次の優先順位で判断する。

1. 現在のユーザー依頼と合意した受け入れ条件
2. 現在のコード
3. `prisma/schema.prisma`
4. 現在のバリデーションとテスト
5. `package.json` のスクリプト・依存バージョンと `package-lock.json`
6. 現在のリポジトリ内ドキュメント
7. 過去のメモや引き継ぎ（補助情報としてのみ使用）

- 確認済みの事実・仮説・未確認事項を区別する。存在しない機能やコマンドを推測で記載しない。
- 認証は現在のClerk実装を基準にする。古いNextAuth前提を復活させない。
- 下位ディレクトリに指示ファイルがある場合は、対象ファイルに適用される内容も確認する。

### Context7によるドキュメント確認

ライブラリ・フレームワーク・SDK・API・CLI・クラウドサービスの使い方、設定、移行、固有の不具合を調べる際は、Context7 MCPを使用する。

1. ライブラリ名と具体的な調査内容で `resolve-library-id` を実行する。ユーザーが正確な `/org/project` 形式のIDを指定した場合のみ省略する。
2. 名前・説明の一致、情報源の信頼性、サンプル数、評価を見て選ぶ。バージョン指定があれば対応するIDを優先する。
3. 選んだIDで `query-docs` を実行する。概念ごとに質問を分け、概念間の連携を調べる場合のみまとめる。
4. 取得した資料と実際の依存バージョンを照合して実装・回答する。

一般的なプログラミング概念、業務ロジックのデバッグ、コードレビュー、ライブラリ固有の仕様確認を伴わないリファクタリングやスクリプト作成では不要。利用できない場合はその事実を報告し、必要に応じて公式資料で補う。未実施の確認を実施済みと記載しない。

## 技術構成と配置

現在の構成はNext.js App Router、React、TypeScript、Tailwind CSS、Clerk、Prisma、PostgreSQL（Neon）、Vercel、Vercel Blobを前提とする。正確なバージョンは依存ファイルで確認する。

HonoとNext.js Route Handlersは既存の使い分けに従い、依頼なしにAPI方式を全面移行しない。

| 配置 | 主な役割 |
| --- | --- |
| `app/` | ルート、レイアウト、ページ、Route Handlerなどの入口 |
| `features/` | 機能別のUI、ロジック、スキーマ、サーバー処理、型 |
| `components/` | 複数機能で実際に共有するコンポーネント |
| `lib/` | 横断的な基盤処理・ユーティリティ |
| `prisma/` | DBスキーマ、マイグレーション、DB用スクリプト |
| `types/` | 共有する型宣言 |
| `public/` | 静的ファイル |
| `scripts/` | 保守・運用スクリプト |
| `tests/` | 自動テスト |

機能固有のコードは既存の `features/` 配下を優先する。見栄えだけを理由にファイルを移動したり、新しいトップレベルディレクトリを増やしたりしない。

## アレルゲン情報の必須ルール

状態の意味は `prisma/schema.prisma` と `lib/allergens.ts`、品目の定義は `lib/constants/allergen-master.ts` を確認する。導出できる品目数を古い固定値で実装しない。

内部状態・表示分類・未入力の意味は [共通ルール](docs/guide/rules.md#allergens)、公開条件は [公開ルール](docs/guide/rules.md#publication) を正本として説明する。以下は変更時に必ず守る制約である。

- `UNKNOWN`、`null`、レコード欠損、空値を `FREE` として扱わない。
- 「未入力」を「含まない」に変換しない。
- `CONTAINS` や `MAY_CONTAIN` を安心させる要約や表示の裏に隠さない。
- 選択対象の全アレルゲンが現在の確認条件を満たさない限り、問題がないと受け取られる結果を表示しない。
- 「安全に食べられる」と保証しない。`FREE` を厨房での交差接触まで確認済みという意味に拡張しない。
- 登録状態の `status` と、店舗内の取扱い情報を反映する `effectiveRisk` を混同しない。`STORE_HANDLED` の補足表示を消したり、DBのアレルゲン状態へ置き換えたりしない。
- 公開条件・注意表示は既存の共通処理を再利用し、画面とAPIで意味を一致させる。

状態の意味や安全性に関わる公開文言を変更する場合は、明示的な製品方針の判断を求める。

## 認証・認可

認証と認可は別々に確認する。保護対象の処理では、次を守る。

- サーバーでClerkセッションを検証する。
- 信頼できるサーバー側の情報から、ユーザーと店舗の関係を解決する。
- 操作対象の店舗・メニュー・画像・レコードの所有権を検証する。
- クライアントが渡す `userId`、`shopId`、`storeId`、`menuId`、ロール、所有権フラグだけを信用しない。
- 可能な限り、DBの読み書きをリソースIDと所有権の両方で絞り込む。
- `proxy.ts`、ミドルウェア、ルート保護、サーバー側の所有権確認を弱めない。
- Clerkの秘密鍵、DB接続文字列、Blobトークンなどをクライアント側へ公開しない。

## 入力検証・API契約

ルートパラメータ、クエリ、フォーム、JSON、ローカルストレージ、アップロード、外部APIレスポンスを含め、すべての外部入力を検証対象とする。

- 既存のZodスキーマや検証方式を再利用し、DBアクセス前に検証する。
- 明示的な変更依頼がない限り、レスポンス形式と既存の契約を維持する。
- スタックトレース、SQL内部情報、秘密情報、不要な内部識別子を返さない。
- 契約変更が承認された場合は、すべての利用箇所・関連テスト・文書を更新する。

## Prisma・データベース

明示的な依頼なしに、`prisma/schema.prisma` の変更、マイグレーションの作成・実行、本番データの変更を行わない。

スキーマ変更が承認された場合は、以下を実施する。

1. アプリ側の変更だけでは解決できない理由を説明する。
2. 影響するモデル・リレーション・制約・既存データを特定する。
3. データ移行とロールバックの方針を示す。
4. 入力検証、API、UI、テスト、関連文書を更新する。
5. Prismaの検証とClient生成を実行する。

- 根拠なく危険な生SQLを使用しない。
- 一括で成功・失敗させるべき更新にはトランザクションを使用する。
- 所有権と一意性の制約を維持し、明らかなN+1クエリを避ける。
- 公開レスポンス用の取得では必要な項目だけを選択する。
- 共有・本番DBに破壊的なリセットを実行しない。
- seed、補正、移行、テスト初期化スクリプトは、名前だけで読み取り専用と判断しない。実装・接続先・副作用を確認してから、許可された対象にのみ実行する。

## Next.js・React

Server Componentsを基本とし、状態・副作用・イベント・ブラウザAPI・ローカルストレージ・位置情報・クライアント専用ライブラリが必要な範囲だけに `"use client"` を付ける。

- 一部の操作UIのためにページやレイアウト全体をClient Componentへ変えない。
- シリアライズできない値をClient Componentへ渡さない。
- 鮮度要件や無効化を理解せず、`force-dynamic`、`no-store`、`revalidate`、キャッシュAPIを追加しない。
- 実装を確認できないISRやキャッシュ最適化を実装済みと報告しない。
- 明示的な依頼なしに公開URL、ルートパラメータ、リダイレクト、クエリの挙動を変更しない。

ルート変更時は、リンク、リダイレクト、`proxy.ts`／ミドルウェアの対象条件、API利用箇所、QRリンク、共有機能を確認する。

## UI・アクセシビリティ

リデザインの依頼がなければ、既存の見た目を維持する。

- モバイル・デスクトップの両方に対応する。
- キーボードフォーカスを見える状態にし、意味に合ったHTML要素と入力欄に関連付けたラベルを使う。
- アレルゲン状態は色だけに頼らず、文字やアイコンでも伝える。
- 読み込み中、空、エラー、無効、成功の各状態を維持する。
- 必要な箇所で二重送信を防ぎ、破壊的操作には明確な確認を設ける。
- 未保存入力の保護、初回利用時の検索・設定操作を変更する場合は、関連する既存の回帰確認も行う。

法的・医療的・安全性に関する文言は、明示的な承認と適切な専門家レビューなしに変更しない。

## 画像

画像アップロードを変更する場合は、認証・所有権確認をサーバー側に保つ。

- MIMEタイプ、サイズ、既存の制約を検証する。
- ファイル名やクライアントから渡されたURLを信用しない。
- 既存のVercel Blob連携を使用し、認証情報を公開しない。
- DB更新の失敗、画像差し替え、参照されなくなったファイルの後処理を考慮する。

## 検索・位置情報

明示的な変更依頼がなければ、次の挙動と組み合わせを維持する。

- 登録店舗のみを対象にした検索
- キーワード処理、都道府県・市区町村の絞り込み
- 完全一致・部分一致の表示順と住所による補完
- 選択アレルゲンによる絞り込み
- 位置情報による並び替え

位置情報が拒否された場合や非対応ブラウザでも通常検索を壊さない。意図的に削除された外部店舗検索・地図APIを勝手に復活させない。

## 依存関係・設定・秘密情報

- 明示的に必要とされ、既存依存では合理的に対応できない場合を除き、パッケージの追加・削除・更新を行わない。
- 依存変更前に `package.json` と `package-lock.json` を確認し、必要性を説明する。既存のnpmでロックファイルを更新し、フルビルドを実行する。
- 承認なしに環境変数名やVercel・Neon・Clerk・Blobの設定を変更しない。
- `.env` ファイルや秘密情報をコミットしない。認証情報をログ・文書・回答へ出力しない。
- シェルの違いによる実行エラーと、コードの不具合を区別する。READMEのBash構文をPowerShellへそのまま渡さない。

## Git・作業範囲

- 無関係な未コミット変更を破棄・上書き・一括整形しない。
- 明示的な依頼なしに、commit、push、merge、force-push、`git reset --hard`、変更を破棄するcheckoutを実行しない。
- pushはユーザーの明示的な指示がある場合に実行する。修正やcommitだけの依頼をpushの許可と解釈しない。
- 1つの作業では1つの目的に集中し、無関係な整理、依存更新、改名、ディレクトリ再構成を混ぜない。
- 範囲外の変更が必要になった場合は、その理由、影響するファイル・契約、リスク、小さな代替案を示して判断を求める。

## 作業手順と検証

単純な修正を除き、次の順で進める。

1. **理解**：変更対応表と関連仕様を読み、現在の実装、データの流れ、権限、曖昧な点を調べる。
2. **計画**：変更ファイル、維持する挙動、リスク、検証方法を伝える。
3. **実装**：既存の規約に従い、目的を満たす最小限の変更と関連文書の更新を行う。
4. **検証**：影響するテストと必要な静的検証・ビルドを実行する。
5. **レビュー**：`git diff --check` と `git diff` を確認し、受け入れ条件と照合する。コード・テスト・文書の整合、目次・相互リンク・コード参照、文書更新の残りを確認する。最後に `git status --short` で変更範囲を確認する。

`package.json` を確認し、存在するスクリプトだけを実行する。Windowsでは `npm.cmd`／`npx.cmd` を使用する。コード変更時の通常の最低限の検証は以下。

```powershell
npm.cmd run lint
npm.cmd run typecheck
npm.cmd test
npm.cmd run build
```

`build` の前には `prebuild` によりPrisma Clientが生成される。PrismaスキーマやPrisma依存コードを変更した場合は、必要に応じて以下も実行する。

```powershell
npx.cmd prisma validate
npx.cmd prisma generate
```

文書のみの変更で実行コード・設定に影響がない場合は、記載内容と実装の照合、差分確認を行い、lint・型検査・テスト・ビルドは省略できる。省略した項目と理由を報告する。

### ブラウザ・手動確認

- 自動テストやビルドの成功だけで、ブラウザ動作も確認済みとしない。
- UI変更ではモバイル／デスクトップ、各表示状態、キーボード操作、関連するアレルゲンの組み合わせを確認する。
- 認証・認可に関係する変更では、未認証ユーザーと権限のないユーザーの操作も確認する。
- 既存のブラウザ回帰スクリプトは `scripts/check-first-use-browser.mjs` と `scripts/check-test-browser.mjs`。実行前にREADMEとスクリプト本体を読み、接続先、専用テストデータ、既存のPlaywright・Chromium、Windowsでの実行条件を確認する。
- 専用テスト環境の初期化や外部サービスへの書き込みは、副作用と対象が依頼の範囲内であることを確認する。ブラウザ検証のためだけに無断で依存パッケージを追加しない。
- 実行できない項目は理由とともに `UNVERIFIED`（未確認）として残す。実行環境の不足をテスト成功と扱わず、過去の成功件数を今回の結果に流用しない。

## 完了報告

原則として日本語で、変更規模に応じて簡潔に報告する。

1. 実装の変更内容と理由（文書のみなら実装変更なしと明記）
2. 更新した文書・ファイルと理由、関連文書を更新不要と判断した場合はその理由
3. 主な受け入れ条件の `PASS`／`FAIL`／`UNVERIFIED`
4. 実行したコマンドと実際の結果（テストは件数も記載）
5. 実行しなかった検証と理由
6. 未確認事項、不一致、残課題、残る手動確認、前提、リスク

未実行のコマンドを成功と報告しない。必要なビルド・型検査・認可・手動動作が未確認の場合は、実装完了と検証完了を区別する。

## 判断を求める条件

次の変更が必要で、現在の依頼や会話でまだ承認されていない場合は、推測で進めず判断を求める。すでに明示された許可を繰り返し求める必要はない。

- アレルゲン状態の意味の変更
- 注意表示や公開条件の緩和
- 認証・所有権ルールの変更
- DBスキーマや本番データの変更
- 公開URLやAPI契約の変更
- 外部サービスや依存パッケージの追加
- ストレージ提供元の変更
- 破壊的なGit・DB操作
- 意味の異なる複数の製品仕様からの選択
